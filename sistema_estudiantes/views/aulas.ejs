<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <h2>Aulas</h2>
    <ul>
      <!-- <% if (aulas.length >0){%> <% for(let i = 0 ; i < aulas.length; i++)<{%>
      <li><%- aulas[i] nombre %></li>
      <%}%> <%}else{%>
      <li>no hay aulas registradas</li>
      } <%}%> -->

      <% if (aulas.length > 0) { %> <% for (let i = 0; i < aulas.length; i++) { %>
      <li><%- aulas[i].nombre %></li>
      <% } %> <% } else { %>
      <li>no hay aulas registradas</li>
      <% } %>
    </ul>

    <form action="/crear-aula" method="POST">
      <input type="text" placeholder="nombre aula" name="nombre" />
      <input type="submit" value="añadir aula" />
    </form>
    <p>volver al <a href="/">inicio</a></p>
  </body>
</html>
